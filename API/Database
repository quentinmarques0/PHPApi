<?php

  class DB {

    public static function parse($file)
    {
      $select = '->';
      $from = '@';
      $where = ':';
      $and = '&';
      $or = '|';
      $test = "";
    }

  }

  class Database extends DB {}
